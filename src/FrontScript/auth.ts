import { mainUrl } from "../config/conf";

// async function faLogin(data: { login: string, pswd: string }): Promise<any> {
//     // Формируем запрос
//     const response = await fetch(`${mainUrl}/auth/login`, {
//         // Метод, если не указывать, будет использоваться GET
//         method: 'POST',
//         // Заголовок запроса
//         headers: {
//             'Content-Type': 'application/json'
//         },
//         // Данные
//         body: JSON.stringify(data)
//     });

//     return response.json();
// }


function myScript() {
    alert(" ТЫНЦ!");
}

